# maf-project: план розвитку

_Аналіз станом на 2026-09-22. Останній коміт — червень 2025._

## Поточний стан

**Що це:** платформа для клубів спортивної мафії: клуби, майданчики, гравці, ігри з типами,
ролями та діями, публічний сайт.

| Частина | Стек | Стан |
| --- | --- | --- |
| Бекенд | NestJS 11, GraphQL (Apollo, code-first), TypeORM, MySQL 8, JWT + argon2 | 17 модулів з CRUD, реєстрація та вхід. Ігрової логіки немає |
| Фронтенд | Next.js 15, React 19, next-intl (uk/en), Tailwind, shadcn/ui, Storybook | Каркас: хедер, тема, перемикач мови. **Усі 8 сторінок порожні**, з API не з'єднаний |
| Інфраструктура | Docker Compose (dev/prod), makefile | Є dev-оточення; prod-конфіг не в git |
| Якість | ESLint, Prettier | Тестів немає (лише шаблонний e2e), CI немає |

### Незакомічена робота
Міграція на **next-intl v4 + Next 15.3** (13 змінених і 3 нові файли) лежить незакоміченою з
червня 2025. Її треба або завершити, або відкотити — це перший крок.

### Знайдені проблеми

**Безпека (критично перед будь-яким деплоєм)**
- JWT-гард стоїть лише на `updateUser`. Решта мутацій відкрита для всіх:
  `removeUser`, `createGame`/`deleteGame`, CRUD клубів, ролей тощо, а також `initializeDatabaseClubs`,
  яка завантажує дані з зовнішнього URL і пише файли на диск.
- `RolesGuard` і `@Roles` написані, але ніде не використовуються.
- `updateUser(id)` не перевіряє, що користувач змінює саме себе.
- Запит `users` без авторизації віддає email усіх користувачів.
- Дефолти в конфігах: `JWT_SECRET || 'secret'`, `MYSQL_PASSWORD || 'password'`. Якщо змінна
  середовища не задана, застосунок мовчки стартує з небезпечними значеннями.
- `email` не має unique-обмеження → можна зареєструвати дублікати.
- Вхід із неіснуючим email повертає «Entity not found», а з неправильним паролем —
  «Invalid Credentials». За різницею відповідей можна з'ясувати, які email зареєстровані.
- `signup` приймає `clubId`, тобто будь-хто може одразу вступити в будь-який клуб.
- `console.log` з `userId` у `JwtStrategy`.

**Архітектура та дані**
- `synchronize: true` замість міграцій. Для продакшну це ризик втрати даних.
- Немає DataLoader: зв'язки в GraphQL підтягуються масивами `relations`, що дає N+1 запитів і
  перевантаження на списках.
- Немає пагінації в жодному списку.
- `Player.username` дублює `User.username`.
- Сервіси дублюють перевірки (`findOne` вже кидає 404, а далі код знову перевіряє `!game`).

**Інфраструктура**
- `docker-compose.prod.yml` у `.gitignore`, тож прод-конфігурація не версіонується.
- Кореневий `package.json` посилається на видалений `docker-compose.dev.yml`.
- `node_modules` на хості неповні (не запускаються навіть `tsc`/`eslint`), тож усе працює
  лише в Docker.
- Немає `.env.example`.
- Залежностям 15+ місяців.

---

## План

### Етап 0. Відновлення робочого стану (1–2 дні)
- [x] Завершити або відкотити міграцію на next-intl v4 і закомітити
- [x] Прибрати зайве з кореневого `package.json`, додати `.env.example` для кореня, бекенду й фронтенду
- [x] Перевстановити залежності; `npm run build`, `lint`, `typecheck` мають проходити локально
- [x] Оновити залежності в межах мажорних версій; Apollo Server 4 → 5 (v4 більше не підтримується),
      Storybook 8 → 10 (v8 несумісний з Next 15.5)
- [ ] Окремо: мажорні оновлення — NestJS 12, Next 16, Tailwind 4, TypeScript 7
- [x] Видалити демо-stories Storybook (`stories/Button`, `Header`, `Page`)
- [x] Підключити стандарти code-agent: сабмодуль `.standards` + `AGENTS.md`
- [x] GitHub Actions: lint + typecheck + test + build для обох частин

### Етап 1. Безпечний і надійний бекенд (1–2 тижні)
**Авторизація**
- [x] Глобальний `GqlJwtGuard` + декоратор `@Public()` для відкритих запитів
- [x] Глобальний `RolesGuard`; довідники змінює лише `ADMIN`, ігри та дії — `HOST` (адмін проходить будь-яку перевірку ролі)
- [x] Перевірка власника для `updateUser`; запит `me`, мутація `setUserRole` (лише адмін)
- [ ] Перевірка власника клубу — після того, як з'явиться роль `club_admin` (етап 4)
- [x] Роль `host` (ведучий/суддя). `club_admin` — разом із клубними функціями
- [x] Однакова помилка входу для неіснуючого email і невірного пароля, схожий час відповіді
- [x] Прибрати `clubId` із `signup` і `updateUser`; вступ до клубу — окремий процес (етап 4)
- [ ] Refresh-токени в httpOnly cookie — разом з автентифікацією на фронтенді (етап 3), бо формат сесії залежить від неї
- [x] Rate limit: 5 спроб/хв на `signIn`/`signup`, 300 запитів/хв загалом

**Конфігурація та дані**
- [x] Валідація env при старті, без небезпечних дефолтів
- [x] Інструменти міграцій (`migration:generate/run/revert/show`)
- [x] Початкова міграція, `synchronize` вимкнено, міграції застосовуються під час старту
- [ ] Сіди для довідників (типи ігор, ролі, дії) — разом з регламентом (етап 2)
- [x] Unique на `users.email`. Індекси на зовнішні ключі MySQL (InnoDB) створює автоматично
- [x] `initializeDatabaseClubs` → `npm run seed:clubs -- <url>`, прибрано з публічного API

**GraphQL**
- [ ] DataLoader — коли фронтенд почне робити вкладені запити. Зараз зв'язки підтягуються JOIN-ами через `relations`, N+1 немає, є лише зайві дані
- [x] Offset-пагінація для списків (users, players, games, clubs, actions)
- [x] `email` бачать лише власник і адмін; пароль ніколи не вибирається з БД, крім входу
- [x] Обмеження глибини запитів (8)
- [x] Єдиний формат помилок з кодами; логер pino

**Тести**
- [x] Unit-тести: env, guards, auth, users, games, форматування помилок, типи ігор (59 тестів)
- [ ] Unit-тести players
- [x] Smoke-тест на запущеному застосунку (33 сценарії доступу) — знайшов 3 баги, які не ловили unit-тести
- [ ] e2e-тести GraphQL на тестовій БД (testcontainers) — потрібен Docker

### Етап 2. Ігровий рушій — ядро продукту (2–4 тижні)
Рішення: **класична клубна мафія**, склад ролей налаштовується типами ігор; усе вносить
**ведучий**; ролі роздаються випадково або вручну (картки за столом).

- [x] Ролі мають команду (місто / мафія / нейтрал), типи дій — ефект (вбивство, лікування,
      перевірка, блокування, голос, нотатка). Нові ролі додаються даними, без коду
- [x] Сід-міграція: мирний, комісар, лікар, путана, мафія, маніяк; типи ігор на 10 і 12 гравців
- [x] Стани: `waiting → in_progress (night N ⇄ day N) → finished | cancelled`
- [x] Розсадка (автоматична або на вказане місце), роздача ролей випадково або вручну
      з перевіркою складу
- [x] Ніч: дія дозволена ролі (`RoleAction`) і фазі; гравці живі; одна дія на гравця;
      мафія стріляє один раз за ніч, маніяк окремо. Розв'язання: блок → лікування → вбивство → перевірка
- [x] День: голосування; нічия повертається ведучому або вирішується «вибувають усі» / «залишаються всі»
- [x] Фоли з лімітом типу гри (за замовчуванням 4) → дискваліфікація
- [x] Перемога міста / мафії / маніяка, завершення гри, 1 бал переможцям + бонуси від ведучого
- [x] Ролі приховані від гравців і глядачів до кінця гри; журнал дій — лише ведучому
- [x] Доменна логіка — чисті функції (30 тестів) + e2e цілої гри на MySQL у CI
- [ ] Промови й таймери — у панелі ведучого (етап 3), бекенду вони не потрібні
- [ ] Subscriptions для оновлення в реальному часі — коли з'явиться екран для глядачів;
      ведучий працює з одного пристрою і отримує стан у відповіді кожної мутації
- [ ] Обмеження лікаря (не лікувати себе двічі поспіль тощо) — налаштуваннями типу гри, коли знадобиться
- [ ] Переписати `GameTypesService.update`: зараз пише в БД до валідації і не змінює `count` у наявних ролей

### Етап 3. Фронтенд MVP (2–3 тижні, частково паралельно з етапом 2)
Архітектура: GraphQL викликає лише сервер Next.js (server components + server actions); JWT
лежить в httpOnly-cookie і в браузер не потрапляє; типи — graphql-codegen зі `schema.gql`.

- [x] graphql-codegen (типи з `backend/src/schema.gql`), перевірка актуальності в CI
- [x] Вхід, реєстрація, вихід; сесія в httpOnly-cookie; `/host` лише для HOST/ADMIN
- [x] **Панель ведучого:** створення гри, розсадка з пошуком, ролі (випадково / з карток),
      ніч, день із нічиєю, фоли, підсумки й бонуси, журнал подій
- [x] **Гравці:** пошук, профіль зі статистикою та історією ігор
- [x] **Календар** найближчих ігор, **сторінка гри** (ролі — лише після завершення)
- [x] **Рейтинг** за весь час / рік / місяць; головна з найближчими іграми й топ-5
- [x] Переклади uk/en, стан завантаження, метадані сторінок
- [ ] Запис гравця на гру з календаря (зараз садить ведучий)
- [ ] Фільтр рейтингу за клубом — разом із клубними функціями (етап 4)
- [ ] «Про нас», «Контакти» — потрібен текст від клубу
- [ ] Промови й таймери в панелі ведучого
- [ ] Refresh-токени: зараз сесія = час життя токена (`JWT_EXPIRES_IN`, рекомендовано 12h);
      вихід не відкликає вже виданий токен
- [ ] Автотести фронтенду (Playwright) — зараз панель і сторінки перевірені вручну в браузері

### Етап 4. Клубні функції (3–4 тижні)
- [ ] **Турніри:** сутність, етапи, розсадка за турами, підсумкова таблиця
- [ ] **Рейтинг:** формула (Elo або бали за регламентом), перерахунок після кожної гри
- [ ] Клуби: сторінка клубу, учасники, розклад, адмінка власника клубу
- [ ] **Блог:** MDX або headless CMS — не варто писати свою CMS
- [ ] Завантаження файлів (аватари, логотипи) в S3-сумісне сховище замість локальної папки

### Етап 5. Продакшн (1 тиждень)
- [ ] Версіонувати `docker-compose.prod.yml`, секрети — лише з env
- [ ] Виправити розбіжності prod-конфігу: у `.env.prod` є `MYSQL_NAME` і `JWT_EXPIERES_IN`,
      а compose чекає `MYSQL_DATABASE` і `JWT_EXPIRES_IN`; порт бекенду `5000:3000`, хоча застосунок
      слухає `BACKEND_PORT` (4000); `NEXT_PUBLIC_API_URL` вшивається під час збірки, а не під час запуску
- [ ] Reverse proxy (nginx/Caddy) + HTTPS
- [ ] Бекапи MySQL за розкладом, перевірка відновлення
- [ ] Healthcheck-и, моніторинг помилок (Sentry), логи
- [ ] CD: деплой з `main` після проходження CI

---

## Рішення, які потрібно ухвалити

1. **Регламент гри:** ФІІМ чи власні правила клубу? Від цього залежать етап 2 і формула рейтингу.
2. **Головний користувач першої версії:** ведучий, який веде гру (панель ведучого), чи гравець,
   який дивиться рейтинг і календар? Від цього залежить порядок етапів 2 і 3.
3. **Один клуб чи багато:** платформа для багатьох клубів вимагає ізоляції даних і ролей на рівні клубу.
4. **Хостинг:** VPS + Docker чи хмарний сервіс (фронт на Vercel, бекенд і БД окремо)?
