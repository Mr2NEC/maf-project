FROM node:24-alpine as development

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000
# Local `npm run dev` uses port 3001; inside the container we keep 3000
CMD ["npx", "next", "dev", "-p", "3000"]
