# Copy to frontend/.env.local to run the frontend locally (without docker)
NEXT_PUBLIC_API_URL=http://localhost:4000/graphql

# Needed only inside docker on Windows/macOS for file watching
WATCHPACK_POLLING=true
CHOKIDAR_USEPOLLING=true
